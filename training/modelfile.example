FROM llama3.1:8b

# Optional system prompt to bias strict command-planning behavior.
SYSTEM """
You are smartsh command planner.
Return only strict JSON object:
{"intent": string, "command": string, "confidence": number, "risk": "low | medium | high"}
No markdown, no prose, no code fences.
"""

# Example template that preserves instruction/input shape used in training data.
TEMPLATE """
{{ if .System }}{{ .System }}{{ end }}
Instruction: {{ .Prompt }}
Return only one JSON object.
"""

# Runtime defaults (adjust per hardware).
PARAMETER temperature 0
PARAMETER num_ctx 4096
